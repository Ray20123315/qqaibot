{
  "name": "qqai-bot-worker",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node verify-config.mjs && node verify-migration.mjs && node verify-model-routing.mjs && node --check worker.js",
    "deploy": "wrangler deploy --config wrangler.toml --no-assets"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
